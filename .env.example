# Copy to .env and adjust. Every value is optional; the defaults in
# bias_audit/config.py are used when a variable is unset.

# Where the UCI Adult dataset is downloaded to and cached.
BIAS_AUDIT_DATA_DIR=./data

# Where result tables, figures and the report are written.
BIAS_AUDIT_RESULTS_DIR=./results
